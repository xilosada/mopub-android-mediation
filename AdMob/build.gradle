project.version = '17.1.2.4'
project.ext.networkName = 'admob_native'

apply from: '../shared-build.gradle'

dependencies {
    implementation "com.google.android.gms:play-services-ads:${sdkVersion}"
}
