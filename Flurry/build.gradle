project.version = '11.6.0.2'
project.ext.networkName = 'yahoo'

apply from: '../shared-build.gradle'

dependencies {
    implementation "com.flurry.android:ads:${sdkVersion}@aar"
    implementation "com.flurry.android:analytics:${sdkVersion}@aar"
}
